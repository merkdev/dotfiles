# dotfiles

macOS setup. Configs live here and are symlinked into `$HOME`.

```sh
git clone git@github.com:merkdev/dotfiles.git ~/Code/dotfiles
cd ~/Code/dotfiles && make install
```

Run `make` to see the other targets.
